"""gtm_eval -- measurement tools for GTM AI outputs.

The core (`gtm_eval.metrics`) is standard library only and has no network
dependency, so the numbers this harness reports can be recomputed by anyone
with a Python install and no API key.
"""

from __future__ import annotations

__version__ = "0.1.0"
